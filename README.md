# speech-search-db
A Python application that uses SpeechRecognition (Google Web Speech API) to transcribe real-time voice input, splits it into an array of words, and searches for a specific target word using a custom Breadth-First Search (BFS) algorithm. If the target word is found, the transcribed text is automatically logged to a local MySQL database.
